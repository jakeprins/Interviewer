<header><h1><%= @category.name  %></h1></header>
<div class="wrapper">
  <div class="button"><%= link_to 'Back', root_path %></div>
</div>


<div class="box effect1">
  <h2><%= @category.description  %></h2>
    <div class="color">
      <h2>If you are ready to prepare yourself for a <%= @category.name  %> interview,
        hit the start button!</h2>
    </div>
</div>

<%= link_to '<button class="startbutton">Start</button>'.html_safe, category_questions_path(@category.id) %></div>
